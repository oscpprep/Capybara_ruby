# Gemfile

source 'https://rubygems.org'

ruby '3.3.0'

# Add the required gems
group :test do
  gem 'cucumber'
  gem 'rspec'
  gem 'capybara'
  gem 'bigdecimal' # Add this line to address the warning
  gem 'selenium-webdriver' # Add this line to include selenium-webdriver
  gem 'base64'
end
